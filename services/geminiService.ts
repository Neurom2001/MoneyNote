// Gemini service has been removed as per user request.
// This file is kept empty to prevent import errors in case of lingering references, 
// though none should exist in the current codebase.

export const analyzeFinances = async (): Promise<string> => {
  return "";
};